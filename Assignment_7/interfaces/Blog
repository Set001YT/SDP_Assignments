package interfaces;

// Subject interface - what blog must provide
public interface Blog {
    void subscribe(Subscriber subscriber);
    void unsubscribe(Subscriber subscriber);
    void notifySubscribers();
}
